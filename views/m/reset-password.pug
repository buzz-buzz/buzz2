doctype html
html
    meta(charset="utf-8")
    meta(http-equiv="X-UA-Compatible", content="IE=edge, chrome=1")
    meta(name="viewport", content="width=device-width, initial-scale=1.0, maximum-scale=1.0")
    title='Buzz English'
body
    style.
        button {
            font-size: 20px;
            color: #333333;
            border-radius: 4px;
            background-color: #f9b600;
            height: 8vh;
            line-height: 8vh;
            border: none;
        }
        body {
            margin: 0px;
        }
        .head {
            width: 100vw;
            height: 10vh;
            background-color: #f9b600;
            position: absolute;
        }
        .title {
            width: 100vw;
            height: 10vh;
            line-height: 10vh;
            text-align: center;
            font-size: 18px;
            color: #333333;
        }
        .back {
            height: 10vh;
            line-height: 10vh;
            font-size: 30px;
            color: #333333;
            position: absolute;
            top: 0px;
            left: 8vw;
        }
        a {
            text-decoration: none;
        }
        .input-container {
            position: absolute;
            left: 10vw;
            width: 80vw;
            padding-bottom: 1vh;
            border-bottom: 1px solid #aaaaaa;
        }
        .left-row {
            display: inline-block;
        }
        .phone {
            position: absolute;
            top: 25vh;
        }
        input[type=phone],
        input[type=password],
        input[type=text] {
            vertical-align: top;
            font-size: 15px;
            width: 50vw;
            border: none;
            background-color: transparent;
        }
        input[type=phone]:focus,
        input[type=password]:focus,
        input[type=text]:focus {
            outline: none;
            background-color: transparent;
        }
        .label {
            font-size: 15px;
            width: 18vw;
            display: inline-block;
            text-align: left;
        }
        .captcha {
            position: absolute;
            right: 0px;
            bottom: 0px;
        }
        .captcha img {
            width: 19vw;
        }
        .captchabox {
            top: 35vh;
        }
        .verification-box {
            top: 45vh;
        }
        .newpassword {
            top: 55vh;
        }
        .verification-box button {
            width: 19vw;
            position: absolute;
            right: 0px;
            bottom: 1vh;
            height: 5vh;
            line-height: 2.5vh;
            font-size: 12px!important;
            color: white;
        }
        .finish {
            position: absolute;
            top: 65vh;
            left: 10vw;
            width: 80vw;
        }
        button:disabled,
        .button:disabled {
            opacity: 0.5;
        }
    .head
        .title 找回密码
        a.back(onClick="history && history.back()") <
    .body(ng-app="resetPasswordModule")
        form(name="resetPasswordForm", ng-submit="resetPassword()", ng-controller="resetPasswordCtrl")
            .left-row.phone.input-container
                .label 手机
                input(type="phone", placeholder="请输入注册的手机号码", ng-model="signUpData.mobile", ng-required="true", maxlength="11", minlength="11")
            .left-row.captchabox.input-container
                .label 图形码
                input(type="text", placeholder="请输入图形验证码", ng-model="signUpData.captcha", ng-required="true", maxlength="4", minlength="4")
                .captcha(ng-model="signUpData.captchaId", captcha)
            .left-row.verification-box.input-container
                .label 验证码
                input(type="text", placeholder="请输入短信验证码", ng-model="signUpData.verificationCode", ng-required="true", maxlength="6", minlength="6")
                resend-verification
            .left-row.newpassword.input-container
                .label 新密码
                input(type="password", placeholder="请设置您的密码", ng-model="signUpData.password", ng-required="true", minlength="6")
            button.finish(type="submit", ng-disabled="!resetPasswordForm.$valid") 完成
    include ../js
    script(src="/js/formModule.min.js")
    script(src="/js/educationModule.min.js")
    script(src="/js/resetPasswordModule.min.js")