extends ../../m/mobile-layout

block meta
    style.
        html {
            height: auto !important;
            overflow: auto;
            font-size: 1rem;
        }

block js
    script.
        angular.module('spaModule')
            .run(['$rootScope', function ($rootScope) {
            }])
        ;
block content
    script(type="text/ng-template", id="user-account.html")
        div(ng-controller="userAccountCtrl")
            .ui.equal.width.center.aligned.padded.grid
                .row(style="background:gray")
                    .column(style="color:#fff")
                        .icon(style='width: 3rem; height: 3rem; margin: 0 auto;line-height: 3rem; border: 2px solid white; border-radius: 50%;text-align: center;')
                            i.diamond.icon(style='font-size: 2rem; margin: 0 !important;')
                        .title(style='line-height: 1.5rem;') 余&nbsp;额
                        .account(style="font-size:smaller; margin: 0 auto; font-size: 1rem;")
                            i.yen.icon(style='margin: 0 !important;')
                            span(ng-cloak) {{account}}
        div(style="margin:0 10px;")
            button.fluid.positive.ui.button(style="margin-top: 1rem;") 充值
            button.fluid.ui.button(style="margin-top: 1rem;") 提现
            .ui.tertiary.inverted.segment buzzbuzz服务
            .ui.segment
                p.red 抱歉, 目前暂不支持"提现"和"充值"功能哦，敬请期待!
